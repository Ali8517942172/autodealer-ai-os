{
  "name": "Sync Closed-Won Deals to Supabase pgvector",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "deals/closed-won",
        "options": {}
      },
      "id": "1",
      "name": "Webhook - New Deal",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [
        256,
        304
      ],
      "webhookId": "0e2f94c0-a4f6-47b4-8032-feb82a59027b"
    },
    {
      "parameters": {
        "jsCode": "// -- Entry-path resolution ------------------------------------------------\n// Webhook - New Deal -> Verify JWT -> here   (public HTTP; auth enforced)\n// direct / sub-workflow call -> here         (already trusted)\n//\n// $('Webhook - New Deal') THROWS when that node did not run in this execution\n// -- it does NOT return undefined, so `?.` and `||` cannot rescue it, and an\n// Expression field cannot hold try/catch.\nlet webhookJson = null;\ntry {\n  webhookJson = $('Webhook - New Deal').first().json;\n} catch (e) {\n  // non-webhook path -- the trigger never executed.\n}\n\nlet raw;\nif (webhookJson) {\n  // 'Verify JWT' runs with onError = continueRegularOutput, so a rejected\n  // token arrives as data. This endpoint is fire-and-forget (the caller only\n  // ever sees 'Workflow was started'), so a failed execution is the right\n  // signal for an unauthorized attempt.\n  const auth = $input.first().json || {};\n  if (!auth.id) {\n    throw new Error('Closed-won sync aborted: unauthorized. A valid Supabase session token is required in the Authorization header.');\n  }\n  raw = webhookJson;\n} else {\n  raw = $input.first().json;\n}\n\n// -- Normalise the deal ---------------------------------------------------\n// The dashboard posts a closed-won deal as\n//   { lead_email, lead_name, phone, vehicle, sale_price_aed, closed_at }\n// with no deal_id at all, while this node used to demand deal_id and read\n// customer_name / amount_aed. That mismatch aborted every single run.\n// Both vocabularies are accepted now.\nconst d = raw.body || raw.deal || raw;\n\nconst clean = (v) => (v === undefined || v === null || v === '') ? null : String(v).trim();\n\nconst customer = clean(d.customer_name) || clean(d.lead_name) || clean(d.name);\nconst email    = clean(d.lead_email) || clean(d.email);\nconst vehicle  = clean(d.vehicle) || clean(d.vehicle_interest);\nconst amount   = (d.amount_aed !== undefined && d.amount_aed !== null) ? d.amount_aed : d.sale_price_aed;\nconst closedAt = clean(d.closed_at) || clean(d.closedAt);\n\nlet dealId = clean(d.deal_id) || clean(d.dealId) || clean(d.id);\nif (!dealId) {\n  // No id supplied. Derive a DETERMINISTIC one so that re-posting the same\n  // deal overwrites its existing vector row instead of adding a duplicate.\n  const key = email || customer;\n  if (!key) {\n    throw new Error('Rejected: closed-won deal has neither a deal_id nor a lead_email/customer name to derive one from. Keys received: ' + Object.keys(d).join(', '));\n  }\n  dealId = 'auto:' + key.toLowerCase() + '|' + (closedAt || 'no-close-date');\n}\n\nconst parts = [\n  customer ? 'Customer: ' + customer : null,\n  email ? 'Email: ' + email : null,\n  vehicle ? 'Vehicle: ' + vehicle : null,\n  (amount !== undefined && amount !== null && amount !== '') ? 'Deal value: AED ' + amount : null,\n  closedAt ? 'Closed at: ' + closedAt : null,\n  clean(d.notes) ? 'Notes: ' + clean(d.notes) : null\n].filter(Boolean);\n\nconst dealText = parts.join('. ');\nif (!dealText) throw new Error('Rejected: deal ' + dealId + ' produced no text to embed.');\n\nreturn [{ json: { dealId, dealText } }];"
      },
      "id": "2",
      "name": "Format Deal Text",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        464,
        304
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://openrouter.ai/api/v1/embeddings",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "openRouterApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "HTTP-Referer",
              "value": "https://nexuscorp.net"
            },
            {
              "name": "X-Title",
              "value": "NEXUS OS - RAG Embeddings"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ model: 'openai/text-embedding-ada-002', input: $json.dealText }) }}",
        "options": {
          "timeout": 30000
        }
      },
      "id": "3",
      "name": "OpenRouter - Generate Embedding",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.1,
      "position": [
        656,
        304
      ],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "credentials": {
        "openRouterApi": {
          "id": "UUtKrBzkQOztAOzZ",
          "name": "OpenRouter account"
        }
      },
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "const response = $input.first().json;\nconst dealId = $('Format Deal Text').first().json.dealId;\nconst dealText = $('Format Deal Text').first().json.dealText;\n\nconst embedding = response?.data?.[0]?.embedding;\n\n// Never fall back to a zero vector: writing 1536 zeros into pgvector silently\n// poisons the knowledge base and every similarity search after it. Fail loudly.\nif (!Array.isArray(embedding) || embedding.length !== 1536) {\n  throw new Error(\n    'Embedding generation failed for deal ' + dealId + '. ' +\n    'Nothing was written to deals_embeddings. Response: ' +\n    JSON.stringify(response).substring(0, 400)\n  );\n}\n\nreturn [{ json: { dealId, dealText, embedding: JSON.stringify(embedding) } }];"
      },
      "id": "parse-embedding",
      "name": "Parse Embedding Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        864,
        304
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://dsvuoovivysszdoiorch.supabase.co/rest/v1/deals_embeddings",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "on_conflict",
              "value": "deal_id"
            }
          ]
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Prefer",
              "value": "resolution=merge-duplicates,return=minimal"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ '{\"deal_id\":' + JSON.stringify(String($json.dealId)) + ',\"content\":' + JSON.stringify($json.dealText) + ',\"embedding\":' + $json.embedding + '}' }}",
        "options": {}
      },
      "id": "4",
      "name": "Supabase (Postgres) - Upsert Vector",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.1,
      "position": [
        1056,
        304
      ],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 1500,
      "credentials": {
        "supabaseApi": {
          "id": "dv4OeARarErZLHCj",
          "name": "Supabase account"
        }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://dsvuoovivysszdoiorch.supabase.co/rest/v1/audit_log",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Prefer",
              "value": "return=minimal"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ workflow: 'Sync Closed-Won to pgvector', status: 'SUCCESS', lead_name: ($json.lead_name || $json.name || null), lead_email: ($json.lead_email || $json.email || null), summary: 'Completed', logged_at: $now.toISO() }) }}",
        "options": {
          "timeout": 15000
        }
      },
      "id": "audit-rag_sync.jso",
      "name": "Audit Log",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1328,
        304
      ],
      "retryOnFail": true,
      "maxTries": 2,
      "waitBetweenTries": 1500,
      "credentials": {
        "supabaseApi": {
          "id": "dv4OeARarErZLHCj",
          "name": "Supabase account"
        }
      },
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "url": "https://dsvuoovivysszdoiorch.supabase.co/auth/v1/user",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRzdnVvb3Zpdnlzc3pkb2lvcmNoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODM3NTg0MzUsImV4cCI6MjA5OTMzNDQzNX0.YPYzknrhqyzT5W9YeiJLGQcV3JF9BGlL1g0BbTr9l9E"
            },
            {
              "name": "Authorization",
              "value": "={{ $json.headers && $json.headers.authorization ? $json.headers.authorization : '' }}"
            }
          ]
        },
        "options": {
          "timeout": 10000
        }
      },
      "id": "cw-verify-jwt-0001",
      "name": "Verify JWT",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        256,
        128
      ],
      "retryOnFail": false,
      "alwaysOutputData": true,
      "onError": "continueRegularOutput",
      "notes": "Guard for the public webhook path only. 401 arrives as data because onError = continueRegularOutput."
    }
  ],
  "connections": {
    "Format Deal Text": {
      "main": [
        [
          {
            "node": "OpenRouter - Generate Embedding",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "OpenRouter - Generate Embedding": {
      "main": [
        [
          {
            "node": "Parse Embedding Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse Embedding Response": {
      "main": [
        [
          {
            "node": "Supabase (Postgres) - Upsert Vector",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Supabase (Postgres) - Upsert Vector": {
      "main": [
        [
          {
            "node": "Audit Log",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook - New Deal": {
      "main": [
        [
          {
            "node": "Verify JWT",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verify JWT": {
      "main": [
        [
          {
            "node": "Format Deal Text",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "availableInMCP": true,
    "binaryMode": "separate",
    "timezone": "Asia/Dubai",
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all",
    "errorWorkflow": "iYJkh1kztWxZXDbT"
  },
  "_exported_from": {
    "id": "dhy2DDjWUqwuzHLW",
    "active": true,
    "updatedAt": "2026-08-17T16:36:54.857Z",
    "published": true
  }
}